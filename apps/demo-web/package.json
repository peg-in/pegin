{
  "name": "@pegin/demo-web",
  "version": "0.1.0",
  "private": true,
  "description": "PEGIN demo relying party — Vite + React (empty skeleton)",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "lint": "eslint src",
    "test": "echo 'No tests yet — see feat-6'"
  },
  "dependencies": {
    "@pegin/sdk": "workspace:*",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "typescript": "^5.0.0",
    "vite": "^6.0.0"
  }
}
